env:
  - name: FLASK_APP
    value: "api_deploy:app"

services:
  - name: flask-backend
    env: python3
    command: "gunicorn -w 4 -b 0.0.0.0:$PORT api_deploy:app"

  - name: streamlit-frontend
    env: python3
    command: "streamlit run dashbord.py"